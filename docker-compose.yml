services:
  wp-porttest2:
    image: muaeabudhabi/wp-porttest2:latest
    restart: unless-stopped
    labels:
      com.centurylinklabs.watchtower.enable: 'true'
      org.titan.app: wp-porttest2
    environment:
    - NODE_ENV=production
    - LOG_LEVEL=info
    - PUID=99
    - PGID=100
    - UMASK=022
    - DATABASE_URL=postgresql://wp-porttest2:changeme@wp-porttest2_postgres:5432/wp-porttest2
    - REDIS_URL=redis://wp-porttest2_redis:6379
    volumes:
    - /mnt/user/appdata/wp-porttest2/data:/data
    - /mnt/user/appdata/wp-porttest2/config:/config
    logging:
      driver: json-file
      options: &id001
        max-size: 10m
        max-file: '3'
    ports:
    - 3101:3100
    healthcheck:
      test:
      - CMD-SHELL
      - curl -sf http://localhost:3101/health || curl -sf http://localhost:3101/healthz
        || curl -sf http://localhost:3101/ || exit 1
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3
    networks:
    - wp-porttest2_net
    depends_on:
      wp-porttest2_postgres:
        condition: service_healthy
      wp-porttest2_redis:
        condition: service_healthy
  wp-porttest2_postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
    - POSTGRES_DB=wp-porttest2
    - POSTGRES_USER=wp-porttest2
    - POSTGRES_PASSWORD=changeme
    volumes:
    - wp-porttest2_postgres_data:/var/lib/postgresql/data
    networks:
    - wp-porttest2_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-app}
      interval: 30s
      timeout: 5s
      retries: 5
  wp-porttest2_redis:
    image: redis:7-alpine
    restart: unless-stopped
    environment: []
    volumes:
    - wp-porttest2_redis_data:/data
    networks:
    - wp-porttest2_net
    logging:
      driver: json-file
      options: *id001
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 30s
      timeout: 5s
      retries: 5
networks:
  wp-porttest2_net:
    driver: bridge
volumes:
  wp-porttest2_data:
    driver: local
  wp-porttest2_postgres_data:
    driver: local
  wp-porttest2_redis_data:
    driver: local
